{
  "name": "<%= name %>",
  "version": "1.0",
  "manifest_version": 2,
  "description": "<%= description %>",
  "icons": {"48": "icon.png"}<%-if options[:option] %>,
  "options_page": "options.html"<% end %><%-if options[:background] %>,
  "background": {
    "scripts": ["background.js"]
  }<% end %>
}